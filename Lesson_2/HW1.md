# Домашнее задание №1

1) Реализовать любую из быстрых сортировок (__NlogN__ сложность).
2) Реализовать любую из квадратичных сортировок (__N^2__ сложность).
3) Используя выборки случайных чисел (можно использовать `random.randint(a, b)`) разных размеров, постройте график времени сортировки *t = f(N)* для обоих сортировок. Для того чтобы засечь время, используйте `time.time()`. Результаты будут лучше если сделать усреднение нескольких сортировок выборок одного размера.